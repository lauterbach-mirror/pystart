default:
  image: python:3.10
  cache:
    paths:
      - .pip-cache/
  before_script:
    - python --version
    - pip install --upgrade pip

stages:
  - build
  - test
  - publish

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.pip-cache"

build:
  stage: build
  script:
    - pip install build
    - python -m build
  artifacts:
    paths:
      - dist/

test:
  stage: test
  image: python:$VERSION
  script:
    - pip install dist/*.whl
    - python -m unittest discover --start-directory tests/unittests
  parallel:
    matrix:
      - VERSION: ['3.7', '3.10', '3.11', '3.12','3.14']

publish:
  stage: publish
  id_tokens:
    PYPI_ID_TOKEN:
      aud: pypi
  script:
    - pip install twine
    - python -m twine upload dist/*
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
